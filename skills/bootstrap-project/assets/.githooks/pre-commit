#!/usr/bin/env sh
set -eu

node scripts/ensure-branch.mjs --check-only
node scripts/validate.mjs

if command -v codegraph >/dev/null 2>&1 && [ -d .codegraph ]; then
  codegraph index >/dev/null 2>&1 || true
fi
