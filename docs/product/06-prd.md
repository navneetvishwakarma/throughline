---
doc: prd
project: Throughline
status: approved        # draft | approved  — must be `approved` before backlog seeding
updated: 2026-08-13
---

# Throughline — Product Requirements

**Personas Applied:** PM, UX, Security

## Problem & goal

Throughline 0.3.1's coverage gate and generated CI scaffold can report a successful upgrade and passing coverage when neither is true for a pnpm (or any multi-workspace) monorepo. The goal of this release is narrow and mechanical: make every gate this plugin generates fail loudly on malformed or incomplete input instead of silently behaving as if nothing were configured.

## Non-goals

New coverage-tool backends beyond the five already supported (Node/Python/Go/Java/Rust); non-GitHub-Actions CI targets; any UI or dashboard redesign beyond the existing static HTML output; telemetry or usage tracking; a synchronizer/reviewed-override mechanism for CI ownership (seed-only is adopted instead — see ADR below).

## Requirements

> Every requirement carries a stable `REQ-xx` ID. Backlog stories reference these
> IDs via `prd_ref`, which is what makes "is requirement X built and tested?"
> answerable by script. Never renumber a shipped requirement. `Release` tags which
> version wave a requirement belongs to (`v1`, `v2`, ...) — a v2+ change to a shipped
> requirement gets a NEW row tagged to the new release; the shipped row is never edited.

| ID | Requirement | Priority | Acceptance | Release |
|------|-------------|----------|------------|---------|
| REQ-01 | `validate.mjs` and `backlog.schema.json` enforce the full coverage contract's shape (mode/min/command/stacks/targets), unconditionally, regardless of `legacyContractGrace` | P0 | Invalid `coverage.mode`, out-of-range `coverage.min`, blank `coverage.command`, non-string `coverage.stacks` entries, and malformed `coverage.targets` entries (missing fields, duplicate ids) all fail `validate.mjs` with a specific error, even when `legacyContractGrace` is active | v1 |
| REQ-02 | `coverage.mjs` fails safely (exit code 2) on malformed runtime configuration instead of silently disabling enforcement | P0 | An unknown `coverage.mode`, a non-numeric/out-of-range `coverage.min`, an invalid `--threshold`, or an unknown `--stack` all exit 2 with a clear message; only correctly-spelled `off`/`warn` bypass threshold enforcement | v1 |
| REQ-03 | `coverage.mjs` supports explicit `coverage.targets` for monorepo workspaces, running every configured target and aggregating by weighted covered/measurable lines | P0 | Two workspace targets with different report sizes run and aggregate correctly (not an unweighted average); a failing target command or a missing declared report fails the run closed; target paths cannot escape the repository root; `--stack` can select exactly one target; single-root auto-detection is unchanged when `coverage.targets` is absent | v1 |
| REQ-04 | The generated `.github/workflows/throughline.yml` is rendered from the target repo's detected lockfile (pnpm/npm/yarn), never suppresses install/coverage-command failures, and is treated as seed-only project-owned scaffold by `sync-plugin.mjs` | P0 | The rendered workflow includes `permissions: contents: read` and contains no `\|\| true`; a customized or pre-0.3.2 copy is never overwritten and never enters `pendingReview`, but is still reported as differing from the current render; a repo with no `package.json` at all defers seeding rather than baking in a workflow with no install step | v1 |
| REQ-05 | `validate.mjs` requires `release_in_flight` once any epic declares an explicit `release`, and `build-dashboard.mjs` never falls back to an invented `v1` when that data is missing | P1 | An epic with an explicit `release` and no matching `release_in_flight` fails `validate.mjs`; a dashboard rendered against ungated data selects an actual declared release and shows a visible configuration warning instead of a false `0/0` "on track" | v1 |
| REQ-06 | `ensure-branch.mjs --name=main` and `--name=master` are rejected without switching branches, from any starting state including detached HEAD | P0 | Both names exit non-zero and leave the working tree exactly where it was, whether called from a feature branch, from `main` itself, or from detached HEAD | v1 |
| REQ-07 | The plugin version is bumped to `0.3.2` consistently across every manifest and the README | P2 | `package.json`, `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json` (both version fields), `.codex-plugin/plugin.json`, and `README.md` all read `0.3.2`; `npm run doctor` passes | v1 |
| REQ-08 | The gate-driven lifecycle (`define-brief` G1 → `define-product` G2 → `define-design`/`define-architecture` G3/G4 → `define-backlog` G5 → `define-epic` G6 → `implement-epic` → `ship-epic` G7 → `release` G8 → `measure-learn` G9) is enforced as a strict, resumable state machine: `gate.mjs` reading `.throughline/gates.json` is the sole source of truth for gate status, and epic-scoped gates (G6/G7) require an explicit `--subject <epic-id>` so one epic's approval can never satisfy another's | P0 | `node scripts/gate.mjs list` reflects `.throughline/gates.json` exactly; a G6/G7 approval recorded for one epic does not satisfy gate-in for a different epic queried with its own `--subject` | v1 |
| REQ-09 | `docs/engineering/backlog.json` is a validated contract: `validate.mjs` + `backlog.schema.json` enforce field ownership (`status` only via the status adapter, `verify.*` only via implement/coverage), the `notstarted → in_progress → done` / `blocked` status enum, and Definition-of-Ready preconditions (`prd_ref` resolves to a real `REQ-xx`, non-empty testable `acceptance`, all `blocked_by` done) | P0 | A story with a dangling `prd_ref`, empty `acceptance`, or an unmet `blocked_by` fails `validate.mjs`; a compliant backlog passes | v1 |
| REQ-10 | Three mechanical, idempotent entry points cover a project's full scaffold lifecycle without ever touching product content: `bootstrap-project` (new repo), `adopt-project` (brownfield repo that never used throughline), and `upgrade-project` / `sync-plugin.mjs` (bring an existing project's scaffold to the currently installed plugin version) | P0 | Re-running any of the three on an already-current project reports no changes; `sync-plugin.mjs` never overwrites a locally-edited scaffold file without `--force`, and never touches `backlog.json`, `.throughline/` working state, or already-written product/architecture/design docs | v1 |
| REQ-11 | The multi-stack coverage gate auto-detects and runs each target stack's own native coverage tool (Vitest/Jest/`c8` for Node, `coverage.py`, `go test -coverprofile`, JaCoCo for Maven/Gradle, `cargo-llvm-cov`) without reimplementing per-file reporting, and supports `--setup` (non-destructive dependency scaffolding), `--story <id>` (patches `verify.coverage` only), and `--reuse` (re-evaluates a prior run against the current threshold rather than trusting a stored pass/fail) | P0 | Each of the five stacks resolves its tool, runs it, and parses that tool's own native report format into a `covered/total/pct` summary; `--reuse` fails a previously-passing summary once `coverage.min` is raised past it | v1 |
| REQ-12 | No skill or human commits directly to `main`/`master`: every skill's branch check auto-creates and switches to a dedicated branch (`feature/<skill>-<timestamp>`, or a shared `epic/<epic-id>-<slug>` for epic-linked work), and the bundled pre-commit hook (`ensure-branch.mjs --check-only`) hard-blocks any direct commit to `main`/`master` regardless of which skill or human is committing | P0 | `ensure-branch.mjs` no-ops already off `main`; on `main` it creates and switches, reporting the new branch name; the pre-commit hook rejects a commit attempted directly on `main` and allows the same commit once off it | v1 |
| REQ-13 | `build-dashboard.mjs` renders `PROGRESS_DASHBOARD.html` (work board + Planning/gate-pipeline section) entirely from structured files (`backlog.json`, `.throughline/gates.json`, coverage summary) at zero agent-token cost, grouping epics by declared release/phase and deriving epic status/progress from child stories rather than storing it | P1 | The dashboard renders without throwing on a bare scaffold with no data; epic progress always matches a fresh count of child story statuses, never a stored/stale value | v1 |
| REQ-14 | `check-docs.mjs` mechanically validates each doc tier (product/design/architecture) against its template: no unfilled placeholders, valid front-matter status, and cross-references (journey/screen `req_ref`, ADR `superseded-by`) resolve to real ids in the same or a linked doc | P1 | An unfilled template placeholder, an invalid status value, a duplicate `REQ-xx`, or a cross-reference pointing at a nonexistent id all fail `check-docs.mjs`; a fully filled, internally consistent doc set passes | v1 |
| REQ-15 | The core workflow is platform-neutral (skills, templates, Node scripts, the backlog contract, generated `AGENTS.md`) with first-class adapters for Claude Code and Codex and a skills-folder install for Antigravity, so the same gate state (`.throughline/gates.json`, `backlog.json`, `AGENTS.md`) produces the same answer to "what's done, what's next" regardless of which agent picks up the repo | P1 | Claude Code, Codex, and Antigravity installer dry runs each produce a working adapter from the same platform-neutral core; gate/backlog state read via one platform's adapter matches what another platform's adapter reads for the same project | v1 |
| REQ-16 | Story status can be tracked fully offline (local status adapter, written directly by workflow skills) or optionally mirrored to GitHub issues (`gh_issue` written back by `define-epic`/ship skills, status mirrored from issue state) — GitHub is never required for local operation | P2 | A project with no GitHub remote configured completes the full gate pipeline using only the local status adapter; when GitHub mirroring is enabled, story `status` reflects the linked issue's open/closed state rather than being hand-set | v1 |

## Success metrics

A green `node scripts/validate.mjs` / `node scripts/coverage.mjs --check` / generated CI run means what it says: no malformed config or suppressed failure can produce a false pass. Measured by the regression test suite added alongside each requirement (see `docs/superpowers/plans/2026-08-12-throughline-0.3.2-truthful-coverage.md`) — no separate analytics needed for a dev-tool plugin with no telemetry.

## Open questions

- [ ] None open for this release. One deliberate, already-decided deviation from the raw feedback is recorded as an ADR-equivalent note here rather than an open question: `coverage.mjs`'s "no supported lockfile" behavior only hard-fails CI when `package.json` exists without a committed lockfile — a repo with no `package.json` at all (a non-Node stack) defers seeding the CI workflow rather than baking in a permanently broken install step, since this plugin's own multi-stack coverage detection already treats that as a legitimate, non-Node project shape.
- [x] REQ-08 through REQ-16 are a retroactive backfill, added 2026-08-13: they document core product capability that shipped in 0.1.0–0.3.1, before this repo ran its own gate-driven PRD process (that process only started with the 0.3.2 cycle above). They were written from the current codebase, not from a pre-implementation brief. Approved 2026-08-13 (G2, recorded in `.throughline/gates.json`).
